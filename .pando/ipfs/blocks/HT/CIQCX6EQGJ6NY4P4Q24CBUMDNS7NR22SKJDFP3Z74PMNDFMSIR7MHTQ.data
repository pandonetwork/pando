
æÞvar inspect = require('../');
var test = require('tape');

test('inspect', function (t) {
    t.plan(1);
    var obj = [ { inspect: function () { return '!XYZÂ¡' } }, [] ];
    t.equal(inspect(obj), '[ !XYZÂ¡, [] ]');
});
Þ