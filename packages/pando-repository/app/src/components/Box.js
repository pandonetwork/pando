import styled from 'styled-components'
import {
  alignItems,
  display,
  height,
  justifyContent,
  space,
} from 'styled-system'

export default styled.div`
  ${space}
  ${display}
  ${alignItems}
  ${justifyContent}
  ${height}
`
