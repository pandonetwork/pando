import styled from 'styled-components'
import {
  space,
  display,
  alignItems,
  width,
  flexDirection,
  justifyContent,
} from 'styled-system'

export default styled.div`
  ${space}
  ${display}
  ${alignItems}
  ${width}
  ${flexDirection}
  ${justifyContent}
`
